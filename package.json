{
  "name": "tasks-api-layered",
  "version": "1.0.0",
  "description": "Tasks API following layered architecture (controller/service/repository/model/dto) using Node + Express + TypeScript.",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4"
  }
}